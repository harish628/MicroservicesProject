apiVersion: v1
kind: Service

metadata:
  name: admin-dashboard
  namespace: shopflow
  labels:
    app: shopflow
    component: admin-dashboard

spec:
  selector:
    app: shopflow
    component: admin-dashboard

  ports:
    - port: 80
      targetPort: 80

  type: ClusterIP
