apiVersion: v1
kind: Service

metadata:
  name: api-gateway
  namespace: shopflow
  labels:
    app: shopflow
    component: api-gateway

spec:
  selector:
    app: shopflow
    component: api-gateway

  ports:
    - port: 8000
      targetPort: 8000

  type: ClusterIP
