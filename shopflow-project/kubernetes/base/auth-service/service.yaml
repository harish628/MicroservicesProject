apiVersion: v1
kind: Service

metadata:
  name: auth-service
  namespace: shopflow
  labels:
    app: shopflow
    component: auth-service

spec:
  selector:
    app: shopflow
    component: auth-service

  ports:
    - port: 8001
      targetPort: 8001

  type: ClusterIP
