apiVersion: v1
kind: Service

metadata:
  name: mysql
  namespace: shopflow
  labels:
    app: shopflow
    component: mysql

spec:
  clusterIP: None

  selector:
    app: shopflow
    component: mysql

  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
      protocol: TCP
