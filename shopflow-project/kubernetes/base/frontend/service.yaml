apiVersion: v1
kind: Service

metadata:
  name: frontend
  namespace: shopflow
  labels:
    app: shopflow
    component: frontend

spec:
  selector:
    app: shopflow
    component: frontend

  ports:
    - port: 80
      targetPort: 80

  type: ClusterIP
