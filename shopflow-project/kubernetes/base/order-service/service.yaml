apiVersion: v1
kind: Service

metadata:
  name: order-service
  namespace: shopflow
  labels:
    app: shopflow
    component: order-service

spec:
  selector:
    app: shopflow
    component: order-service

  ports:
    - port: 8003
      targetPort: 8003

  type: ClusterIP
