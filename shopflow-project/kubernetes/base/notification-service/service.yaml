apiVersion: v1
kind: Service

metadata:
  name: notification-service
  namespace: shopflow
  labels:
    app: shopflow
    component: notification-service

spec:
  selector:
    app: shopflow
    component: notification-service

  ports:
    - port: 8005
      targetPort: 8005

  type: ClusterIP
